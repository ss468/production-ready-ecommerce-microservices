# Frontend

This is a small Vite + React frontend for the ecommerce microservices repo.

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Build production bundle:

```bash
cd frontend
npm run build
```
